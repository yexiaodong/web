一、轮盘：
整个页面可以分为三个部分：
1.背景图片，包括奖品类别、中奖名单背景，如：bg1.png；
2.旋转的指针图片，如：arrow.png；
3.中奖名单，后台传过来。
代码逻辑：
1.根据奖品种类数n，定义一个数组（0到n-1）。
2.使用Math.floor(Math.random()*data.length)获取0到n-1的随机数。
3.根据获取的随机数，设置指针图片旋转的角度和提示语。
4.使其转动（添加旋转圈数）


二、从名单中抽奖
页面组成:两个按钮（开始、停止），一个文本（显示中奖人）
代码逻辑：
1.将名单放入数组；
2.change方法，改变中奖人（随机）；
3.start方法，使用setInterval设置每间隔多长时间调用一次change方法。
4.end方法，使用clearInterval停止调用change方法。